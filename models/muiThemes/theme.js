import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FEE523',
    },
    secondary: {
      main: '#eff6ee',
    },
    danger: {
      main: '#dd0426',
    },
  },
});

export default theme;
